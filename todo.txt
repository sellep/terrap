 x dynamic sockets
 - extracting type definitions
    x extract switch conf
    x extract hygro conf
    - extract or rather create led conf
    - extract schedule conf
 - renaming switch to socket
 - config validation
    - infinity loop detection
 - cpu flags makefile
    - check for cpuid2cpuflags and use the cpu flags
